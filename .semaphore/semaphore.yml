version: v1.0
name: RadGrad2 Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: 'ESLint'
    task:
      prologue:
        commands:
          # Get the latest code
          - checkout
          # install Meteor
          - curl https://install.meteor.com | /bin/sh
          - cd app
          - meteor npm install
      jobs:
        - name: 'Run Lint'
          commands:
            - cd app
            - meteor npm run lint

  - name: 'Unit Test'
    task:
      jobs:
        - name: Run test
          commands:
            - cd app
            - meteor npm run test
