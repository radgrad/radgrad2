import React from 'react';
import RadGradSegment from '../RadGradSegment';

const CommunityActivity: React.FC = () => (
  <RadGradSegment icon='calendar alternate outline' title='community activity' >
    A feed of changes among users of RadGrad and other entities in the system.
  </RadGradSegment>
);

export default CommunityActivity;
